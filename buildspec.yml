version: 0.2
env:
  secrets-manager:
    LOGIN: x22163654/b61d42c9e8c18514609d9821a34217b9fe3fe9fa:sonartoken
    HOST: x22163654/https://sonarcloud.io:HOST
    Organization: x22163654/bhuvanp96:Organization
    Project: x22163654/Employee_CPP:Project
phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - sudo yum update -y
      -  yum install -y jq
      - wget http://www-eu.apache.org/dist/maven/maven-3/3.5.4/binaries/apache-maven-3.5.4-bin.tar.gz
      - tar xzf apache-maven-3.5.4-bin.tar.gz
      - ln -s apache-maven-3.5.4 maven
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-3.3.0.1492-linux.zip
      - unzip ./sonar-scanner-cli-3.3.0.1492-linux.zip
      - export PATH=$PATH:/sonar-scanner-3.3.0.1492-linux/bin/
  build:
    commands:
      - mvn test     
      - mvn sonar:sonar -Dsonar.login=$LOGIN -Dsonar.host.url=$HOST -Dsonar.projectKey=$Project -Dsonar.organization=$Organization
      - sleep 5
      - curl https://sonarcloud.io/api/qualitygates/project_status?projectKey=$Project >result.json
      - cat result.json
      - if [ $(jq -r '.projectStatus.status' result.json) = ERROR ] ; then $CODEBUILD_BUILD_SUCCEEDING -eq 0 ;fi
post_build:
commands:
- mvn package
- mv target/employee-management-0.0.1.war employee-management-0.0.1.war
artifacts:
files:
- '**/*'